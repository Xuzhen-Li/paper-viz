# Agent trigger — schematic-design

**Command:** `/schematic`

1. Read [SKILL.md](SKILL.md) in this folder.
2. Treat all text after `/schematic` as the diagram task.
3. Brand scrape stays (§0). Quantitative bar/line/scatter/Gantt → `nature-figure`. Paper `.drawio` raster redraw → `drawio-skill`.
4. After choosing a type: `python3 visualization/skill/query.py routes --need <type> --house`.

Install: `~/.cursor/skills/schematic-design` → this folder.
